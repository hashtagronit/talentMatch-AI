/**
 * - job description schema: string
 * - resume text schema: string
 * - self description schema: string
 * - match score schema: number
 * - Technical Questions schema: [{ question: "", answer: "", intention: ""}]
 * - Behavioral Questions schema: [{ question: "", answer: "", intention: ""}]
 * - skjil gaps schema: [{
 *  skill: "",
 *  severity: {
 * type:string,
 * enum:["high","medium","low"]},
 * }]
 * - preparation plan schema: [
 * {
 * day:number,
 * focus:string,
 * tasks:string[]
 * }]
 */